@model IEnumerable<Product>
<h1>Products:</h1>
<p>Count: @Model.Count()</p>

<table>
    <tr><td>Name</td><td>kcal per 100g</td></tr>
@foreach (var p in Model)   
{
    <tr>
        <td>@p.Name</td>
        <td>@(Math.Round(p.Energy/10, 0))</td>
    </tr>
}
</table>
